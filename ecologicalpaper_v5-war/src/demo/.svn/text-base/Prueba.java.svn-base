package demo;

import java.util.StringTokenizer;

public class Prueba {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		Prueba p = new Prueba();
	//	p.metodo();
		System.out.println(System.getProperty("java.io.tmpdir"));
		/*StringBuffer DC = new StringBuffer();
		String ldapdominiodc="ecologicalpaper.com";
        if (ldapdominiodc.indexOf(".")!=-1){
        	System.out.println(ldapdominiodc.indexOf("."));
        
        }
        
        String extension = ldapdominiodc.substring(ldapdominiodc.lastIndexOf(".") + 1, ldapdominiodc.length());
        String nombre = ldapdominiodc.substring(0,ldapdominiodc.lastIndexOf(".") );
        System.out.println(extension);
        System.out.println(nombre);
        DC.append("DC=").append(nombre).append(",DC=").append(extension);
        System.out.println(DC);*/
		

	}
	
	private void metodo() {
		StringTokenizer numerosAValidar = new StringTokenizer("", ",");

		while (numerosAValidar.hasMoreTokens()) {
			String number2 = numerosAValidar.nextToken();
			System.out.println(number2);
		}
	}

}
